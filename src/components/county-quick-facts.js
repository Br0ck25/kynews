import React from "react";

// Placeholder component for county quick facts.  The real implementation was
// removed but tests and the county page expect this module to exist.
// The component simply renders nothing for now.
export default function CountyQuickFacts({ countyName }) {
  return null;
}
