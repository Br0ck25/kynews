// This module previously contained introductory text about each Kentucky
// county (used for SEO).  It was removed in a refactor but some code still
// imports it.  Provide an empty object to avoid import errors.

const COUNTY_QUICK_FACTS = {};

export default COUNTY_QUICK_FACTS;
